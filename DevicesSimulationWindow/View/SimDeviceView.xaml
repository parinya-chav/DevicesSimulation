<UserControl x:Class="DevicesSimulationWindow.View.SimDeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="172" d:DesignWidth="198"
             DataContext="{Binding SimDevice, Source={StaticResource Locator}}">
    
    <Grid Height="173" Width="182">
        <StackPanel>
            <TextBlock Text="Imei" Margin="0,10,0,0" />
            <TextBox Text="{Binding Imei,Mode=TwoWay}" Width="100" Height="25" HorizontalAlignment="Left"/>
            <TextBlock Text="Description" />
            <TextBox Text="{Binding Description,Mode=TwoWay}" Width="100" Height="25" HorizontalAlignment="Left"/>
            <TextBlock Text="Message" />
            <TextBox Text="{Binding SendTotal,Mode=TwoWay}" Width="100" Height="25" HorizontalAlignment="Left"/>
            <TextBlock Text="Status" />
            <TextBox Text="{Binding Status,Mode=TwoWay}" Width="100" Height="25" HorizontalAlignment="Left"/>
        </StackPanel>
    </Grid>
</UserControl>
